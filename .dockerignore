# Only the theme goes into the image; keep everything else out of the build context
*
!theme
!theme/**
!docker-entrypoint-custom.sh
!routes.yaml

# The theme is compiled from source inside the image, so keep build artifacts
# and dependencies out of the context — they're regenerated by `gulp build`.
theme/source-custom/node_modules
theme/source-custom/assets/built
